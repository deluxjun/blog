import React from 'react';
import styled from 'styled-components';

const HeaderBlock = styled.AuthTemplate``;

const Header = () => {
  return <HeaderBlock>Header</HeaderBlock>;
};

export default Header;
